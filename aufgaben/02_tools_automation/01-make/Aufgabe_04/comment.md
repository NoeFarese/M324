Wenn ich ```make docker-run``` ausführe kommt der Fehler "exec format error".

Weil ich es auf dem mac kompiliere erzeugt es ein anderes binary, welchxes debian nicht erwartet.

ich müsste es im dockerfile kompilieren, aber dann wäre die aufgabe nicht gemäss aufgabenstellung umgesetzt.